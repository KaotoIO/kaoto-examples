- route:
    id: route-7542
    from:
      id: from-3743
      uri: direct
      parameters:
        name: From Read CSV
      steps:
        - filter:
            id: filter-3679
            description: Filter by ZIP code
            steps:
              - to:
                  id: to-1576
                  description: Sending to error
                  uri: kafka
                  parameters:
                    brokers: localhost:9092
                    topic: Error-Data
              - stop:
                  id: stop-3684
            simple:
              expression: ${body[ZIP]} == null || ${body[ZIP]} == ""
        - to:
            id: to-1818
            description: Sending to DB
            uri: kafka
            parameters:
              brokers: localhost:9092
              topic: DB-Data
